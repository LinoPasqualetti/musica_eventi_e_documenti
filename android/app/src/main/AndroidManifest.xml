<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Necessario in release: senza questa, WebView/HTTP (es. abcjs da CDN nel
         visualizzatore ABC) non funzionano nell'APK distribuito. Prima era
         presente solo nei manifest di debug/profile, aggiunti automaticamente
         da Flutter solo per il tooling di sviluppo. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Richiesto da Android 13 (API 33) in su per mostrare notifiche locali
         (flutter_local_notifications); senza, le notifiche non compaiono. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="musica_eventi_e_documenti"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Necessaria da Android 11 (API 30) in su perché canLaunchUrl/launchUrl
             possano vedere l'app NotationPlayer tramite il suo schema custom
             (vedi services/notation_player_launcher.dart). Senza questa entry,
             canLaunchUrl restituisce sempre false anche con l'app installata. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="notationplayer"/>
        </intent>
    </queries>
</manifest>
